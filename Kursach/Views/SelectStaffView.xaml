<UserControl 
    x:Class="Kursach.Views.SelectStaffView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    MaxHeight="{Binding Path=ActualHeight, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Converter={StaticResource ActualSizeConverter}}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ListBox ItemsSource="{Binding Staff}"
                 SelectedItem="{Binding SelectedStaff}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding StringFormat='ФИО: {0}'}"/>

                        <TextBlock Text="{Binding Position, StringFormat='Должность: {0}'}"/>

                        <Separator/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <UniformGrid Grid.Row="1"
                     Rows="1">
            <Button Style="{StaticResource CancelDialogButton}"
                    Content="Закрыть"/>

            <Button Style="{StaticResource FlatButton}"
                    Content="Выбрать"
                    Command="{Binding CloseDialogCommand}"/>
        </UniformGrid>
    </Grid>

</UserControl>
