<UserControl 
    x:Class="ISTraining_Part.Views.WelcomeSlides.Slide5"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ISTraining_Part.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}">

    <StackPanel Style="{StaticResource CenterStack}">

        <TextBlock Text="Создатель:"
                   Style="{StaticResource TitleBlock}"/>

        <TextBlock Text="Смирнов Александр"
                   Style="{StaticResource TitleBlock}"/>

        <TextBlock Text="Группа: ИТ-41 (2020г.)"
                   Style="{StaticResource TitleBlock}"/>

        <TextBlock Style="{StaticResource TitleBlock}">
            <Run Text="VK.Com:"/>
            <Hyperlink Command="{Binding OpenVkCommand}">
                <Run Text="id99551920"/>
            </Hyperlink>
        </TextBlock>

        <Button Style="{StaticResource PreviousSliderButton}"/>

    </StackPanel>
</UserControl>
